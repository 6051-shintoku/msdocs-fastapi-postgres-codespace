# Windows環境でのFastAPI + PostgreSQL開発環境セットアップ手順

## 前提条件

以下のソフトウェアをインストールする必要があります：

1. [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
2. [Visual Studio Code](https://code.visualstudio.com/)
3. [Git for Windows](https://gitforwindows.org/)

## セットアップ手順

### 1. Docker Desktopのインストールと設定

1. Docker Desktopをダウンロードしてインストール
2. インストール後、Docker Desktopを起動
3. WSL 2（Windows Subsystem for Linux 2）のインストールを求められた場合は、指示に従ってインストール

### 2. プロジェクトのクローン

1. PowerShellまたはコマンドプロンプトを開く
2. 任意のディレクトリに移動し、以下のコマンドを実行：

```bash
git clone https://github.com/6051-shintoku/msdocs-fastapi-postgres-codespace.git
cd msdocs-fastapi-postgres-codespace
```

### 3. Visual Studio Codeでプロジェクトを開く

1. VS Codeを起動
2. 以下の拡張機能をインストール：
   - Docker
   - Dev Containers
   - PostgreSQL
   - Python（Python IntelliSense、デバッグ、コード整形用）

### 4. Dev Containerでプロジェクトを開く

1. VS Codeのコマンドパレット（Ctrl+Shift+P）を開く
2. `Dev Containers: Open Folder in Container`を選択
3. クローンしたプロジェクトフォルダを選択

これにより、以下の環境が自動的に構築されます：
- **Python 3.11**の開発環境
- **PostgreSQL 18**データベースサーバー
- **必要なPythonパッケージ**（FastAPI、SQLAlchemy、psycopg2など）
- **開発用ツール**（uvicorn、fakerなど）

> **重要**: Dev Container起動時に、PostgreSQLサーバーは自動で開始されますが、アプリケーション用のテーブルはまだ作成されていません。

### 5. アプリケーションの初期化とテーブル作成

#### 5.1 環境設定ファイルの準備

```bash
cp .env.devcontainer .env
```

#### 5.2 FastAPIアプリケーションの起動

初回起動時に、SQLAlchemyがデータベーステーブルを自動作成します：

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

起動成功時のログ例：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process using WatchFiles
CREATE TABLE restaurants (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1 CYCLE), 
    name VARCHAR(100) NOT NULL, 
    address VARCHAR(100), 
    PRIMARY KEY (id)
)
```

### 6. WebアプリケーションとAPIの確認

#### 6.1 メインページの確認

ブラウザで http://127.0.0.1:8000 にアクセス

期待される表示：
```
Welcome to the FastAPI and Postgres in a dev container demonstration. Add /docs to the URL to see API methods.
```

#### 6.2 APIドキュメントの確認

ブラウザで http://127.0.0.1:8000/docs にアクセス

FastAPIが自動生成するSwagger UIが表示され、以下のAPIエンドポイントが利用可能：
- `GET /` - ウェルカムメッセージ
- `GET /restaurant/{id}` - 指定IDのレストラン情報取得
- `POST /restaurant` - 新しいレストラン追加  
- `GET /all` - 全レストラン一覧取得

### 7. データベース操作の確認

#### 7.1 PostgreSQLへの接続

```bash
psql -h localhost -U admin -d postgres
```

パスワード: `LocalPasswordOnly`

#### 7.2 テーブルの確認

```sql
-- テーブル一覧の表示
\dt;

-- 期待される結果
--           List of relations
--  Schema |    Name     | Type  | Owner 
-- --------+-------------+-------+-------
--  public | restaurants | table | admin

-- テーブル構造の確認
\d restaurants;

-- 期待される結果
--                                  Table "public.restaurants"
--  Column  |          Type          | Collation | Nullable |             Default              
-- ---------+------------------------+-----------+----------+----------------------------------
--  id      | integer                |           | not null | generated by default as identity
--  name    | character varying(100) |           | not null | 
--  address | character varying(100) |           |          | 
-- Indexes:
--     "restaurants_pkey" PRIMARY KEY, btree (id)
```

#### 7.3 サンプルデータの挿入（SQLで直接操作）

⚠️ **重要**: PostgreSQLでは文字列は**シングルクォート（'）**で囲みます

```sql
-- 正しい構文
INSERT INTO restaurants (name, address) VALUES ('振徳喫茶', '日南市飫肥123番地');
INSERT INTO restaurants (name, address) VALUES ('田中食堂', '宮崎市中央通り456番地');
INSERT INTO restaurants (name, address) VALUES ('山田レストラン', '都城市早鈴町789番地');

-- データの確認
SELECT * FROM restaurants;
```

#### 7.4 APIを使用したデータ操作

API経由でもデータを操作できます：

```bash
# レストランの追加（POST）
curl -X POST "http://127.0.0.1:8000/restaurant" \
     -H "Content-Type: application/json" \
     -d '{"name": "新富食堂", "address": "新富町富田12-56"}'

# 全レストランの取得（GET）
curl "http://127.0.0.1:8000/all"

# 特定レストランの取得（GET）
curl "http://127.0.0.1:8000/restaurant/1"
```

## トラブルシューティング

### "Did not find any relations"エラーについて

PostgreSQLに接続後、`\dt;`コマンドで"Did not find any relations."と表示される場合：

**原因**: FastAPIアプリケーションがまだ起動されておらず、テーブルの初期化が完了していない

**解決方法**:
1. FastAPIアプリケーションを起動してテーブルを作成：
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

2. 起動後、以下のようなログが表示されてテーブルが作成されます：
```
CREATE TABLE restaurants (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1 CYCLE), 
    name VARCHAR(100) NOT NULL, 
    address VARCHAR(100), 
    PRIMARY KEY (id)
)
```

3. 再度PostgreSQLで確認：
```sql
\dt;
-- restaurants テーブルが表示されるはず
```

### SQLの構文エラーについて

PostgreSQLでは文字列リテラルは**シングルクォート（'）**で囲む必要があります：

```sql
-- ✅ 正しい
INSERT INTO restaurants (name, address) VALUES ('振徳', '日南市飫肥123番地');

-- ❌ 間違い
INSERT INTO restaurants (name, address) VALUES ("振徳", "日南市飫肥123番地");
```

### パッケージ依存関係の問題

Dev Container環境では、必要なパッケージは既にインストール済みですが、追加パッケージが必要な場合：

```bash
# 安全にパッケージを追加（コンテナ内のみに影響）
pip install --upgrade pip
pip install 新しいパッケージ名
```

### Docker Desktopの設定確認

1. Docker Desktopが正常に起動していることを確認
2. タスクトレイのDockerアイコンが緑色になっていることを確認
3. メモリ割り当ては最低4GB推奨（Settings > Resources > Memory）

### ポートの競合が発生する場合

- PostgreSQLのデフォルトポート（5432）が既に使用されている場合は、`.devcontainer/docker-compose.yaml`のポート設定を変更してください。

### データベース接続エラーが発生する場合

1. コンテナが正常に起動していることを確認：
```bash
docker ps
```

2. データベースのログを確認：
```bash
docker compose logs db
```

## Dev Containers環境のメリット

### 環境の隔離性
- **ローカル環境を汚さない**: Dev Container内でのパッケージインストールや設定変更は、ホストマシンのローカル環境に影響しません
- **Python環境の隔離**: pipでのパッケージインストールやアップグレードは、コンテナ内のみに適用されます
- **データベース環境の隔離**: PostgreSQLサーバーもコンテナ内で動作し、ローカルのデータベース設定と干渉しません

### 実例：pipのアップグレード
```bash
# Dev Container内で安全にpipをアップグレード
pip install --upgrade pip
# → ローカルマシンのpip環境には影響なし

# パッケージの追加インストールも安全
pip install numpy pandas matplotlib
# → コンテナ内のみに適用
```

### 開発環境の統一性
- **チーム開発**: 全メンバーが同じPython、PostgreSQL、依存関係のバージョンを使用
- **環境差異の排除**: "私の環境では動くのに..."問題を解決
- **簡単な環境リセット**: コンテナを削除・再構築するだけで初期状態に戻せる

## 補足情報

- このプロジェクトはGitHub Codespacesと互換性のある設定になっています
- ローカルのDocker環境でも同じ開発環境を再現できます
- VS CodeのDev Containers機能により、チーム全員が同じ開発環境を使用できます

## 参考リンク

- [Docker Desktop Documentation](https://docs.docker.com/desktop/windows/)
- [Visual Studio Code Dev Containers](https://code.visualstudio.com/docs/devcontainers/containers)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)